# Student CRUD oprations with Spring boot and  JPA
a simple spring boot resfull api project, gives the ability to do CRUD opration on a student <br />
GET /api/v1/student <br />
POST /api/v1/student <br />
PUT /api/v1/student/{id}?name={name}&email={email} <br />
DELETE /api/v1/student/{id} <br />
